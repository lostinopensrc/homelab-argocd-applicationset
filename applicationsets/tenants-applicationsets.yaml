apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: tenants
  namespace: argo-cd
spec:
  generators:
  - git:
      repoURL: https://github.com/lostinopensrc/homelab-argocd-applicationset.git
      revision: main
      files:
      - path: "tenants-discovery/tenants/grafana/config.json"
  template:
    metadata:
      name: '{{tenant_id}}'
    spec:
      project: default
      source:
        repoURL: https://github.com/lostinopensrc/homelab-argocd-applicationset.git
        targetRevision: main
        path: 'tenants/{{tenant_id}}'
        helm:
          valueFiles:
            - {{tenant_id}}.yaml
      destination:
        server: '{{cluster.address}}'
        namespace: '{{cluster.namespace}}'